import { ArrayDisplayPipe } from './array-display.pipe';

describe('ArrayDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
