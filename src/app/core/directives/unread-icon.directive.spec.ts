import { UnreadIconDirective } from './unread-icon.directive';

describe('UnreadIconDirective', () => {
  it('should create an instance', () => {
    const directive = new UnreadIconDirective();
    expect(directive).toBeTruthy();
  });
});
